# pdfmitra
pdfmitra (पीडीएफ मित्र) — a Claude Code slash command for lossless PDF operations: merge, split, extract pages/text/tables, watermark, rotate, OCR. Install as a Claude Code plugin or drop in ~/.claude/commands/.
